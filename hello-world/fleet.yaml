defaultNamespace: default
namespace: fleetsacan
imageScans:
- policy:
    semver:
      range: "*"
    alphabetical:
      order: asc
  image: "harbor-molmedo.do.support.rancher.space/imagenscan/nginx" # {"$imagescan": "img-scan"}
  tagName: img-scan
  interval: 1m
  secretRef:
    name: scan
